Screensaver module.

Screensaver application itself without web content, caption and icon files.
It is used by build scripts for embedding into ScrGen (screensaver generator) application.

This application could not be started without ScrGen processing since it does not contain web content files.