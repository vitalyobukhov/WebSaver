Win32 resource processor.

ResUpd allows to extract, add, update and delete resources within PE file.
It is used by build scripts to embed Scr (screensaver) module into ScrGen (screensaver generator) one.

Run application without arguments to see usage info.